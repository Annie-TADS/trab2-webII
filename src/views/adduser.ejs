<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= editing ? 'Editar Usuário' : 'Criar Usuário' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">

  <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-lg">
    <h2 class="text-2xl font-semibold text-center text-gray-800 mb-6">
      <%= editing ? 'Editar Usuário' : 'Criar Usuário' %>
    </h2>

    <form action="<%= editing ? '/updateuser/' + user.id : '/adduser' %>" method="POST" class="space-y-4">
      <% if (editing) { %>
        <input type="hidden" name="id" value="<%= user.id %>" />
      <% } %>

      <div>
        <label for="cpf" class="block text-sm font-semibold text-gray-700">CPF</label>
        <input type="text" id="cpf" name="cpf" value="<%= editing ? user.cpf : '' %>" class="w-full mt-2 px-4 py-2 border rounded-md" <% if (editing) { %> readonly disabled <% } %> required />
      </div>

      <div>
        <label for="name" class="block text-sm font-semibold text-gray-700">Nome</label>
        <input type="text" id="name" name="name" value="<%= editing ? user.name : '' %>" class="w-full mt-2 px-4 py-2 border rounded-md" required />
      </div>

      <% if (!editing) { %>
        <div>
          <label for="password" class="block text-sm font-semibold text-gray-700">Senha</label>
          <input type="password" id="password" name="password" class="w-full mt-2 px-4 py-2 border rounded-md" required />
        </div>
      <% } %>

      <div class="flex justify-center mt-6">
        <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded-md">
          <%= editing ? 'Atualizar' : 'Criar' %> Usuário
        </button>
      </div>
    </form>
  </div>

</body>
</html>
